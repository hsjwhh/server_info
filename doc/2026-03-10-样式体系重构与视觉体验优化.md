# 服务器资产管理平台 - 样式体系重构与视觉体验优化

**日期：** 2026-03-10  
**负责人：** Gemini (AI Assistant)  
**分支：** style-optimization-v2

---

## 1. 核心目标

本次改动旨在提升项目的视觉一致性、精致度以及前端样式的可维护性。通过建立完整的 **Design Token** 体系，消灭硬编码（Hardcoding），并对全站核心组件进行针对性的 UI 细节优化。

## 2. 关键改动详情

### 2.1 Design Token 体系补全 (tokens.css)
- **品牌色镜像**：通过 CSS 变量映射 Vuestic `primary` 颜色，方便非组件 CSS 引用。
- **语义化 Token**：新增了 `Sidebar`、统计模块（Statistic）、统计面板（PowerAnalysis）专属的语义颜色变量。
- **动效时长**：定义了全局统一的 `fast` (0.15s)、`base` (0.25s)、`slow` (0.35s) 过渡时长。
- **层级阴影**：补全了从 `sm` 到 `xl` 的阴影阶梯，用于不同高度的卡片层级。
- **排版节奏**：统一了 `line-height` 和 `font-weight` 阶梯，提升阅读体验。

### 2.2 视觉精致度提升
- **Sidebar 升级**：为 Sidebar Logo 区域增加了底部分割线与品牌间距，优化了折叠/展开时的淡入淡出动画效果。
- **PageHeader 强化**：恢复了 PageHeader 的背景色与底部边线，增强了面包屑导航区域的视觉锚点。
- **Dashboard 统计卡片**：为四类统计卡片（总数、在线、告警、离线）增加了左侧彩色 Accent 条，提升信息辨识度。
- **全局卡片阴影**：为 `VaCard` 注入了统一的悬浮升起效果 (`hover-lift`)。

### 2.3 架构优化与代码整洁
- **Utilities 升级**：移除了 `utilities.css` 中的 `!important` 滥用，使样式优先级遵循正常的级联规则。
- **组件样式精简**：从 `ServerEntryPage.vue` 等核心页面中提取了冗余的布局类到全局 `utilities`，大幅减少了 `scoped` 样式的体积。
- **空状态统一**：全站空状态图标尺寸统一缩小（96px -> 64px），降低视觉压迫感，提升页面空间利用率。

### 2.4 缺陷修复
- **Login 页 Hack 移除**：删除了 `Login.vue` 中具有渲染风险的 `clip-path` hack。通过 `overflow: hidden` 与 `z-index` 调整，在保留圆角的同时解决了 `VaInput` 清除按钮被裁剪的问题。

## 3. 影响范围

- **所有页面**：涉及 `Layout`、`Sidebar`、`Header`、`PageHeader` 等全局组件。
- **核心模块**：`Dashboard`、`ServerEntry`、`ServerDetail`、`ConfigPlan`。
- **全局样式**：`style.css`、`tokens.css`、`utilities.css`。

## 4. 验证结论

- **构建验证**：`pnpm build` 通过，编译无异常。
- **兼容性验证**：CSS 变量用法符合主流浏览器规范，排除了 `clip-path` 可能带来的移动端渲染风险。

---

*Note: 本文档由 Gemini 自动生成并同步至 CHANGELOG。*
